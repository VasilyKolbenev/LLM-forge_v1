# Llama 3.2 1B Instruct — lightweight, fast training
# Good for prototyping and resource-constrained environments

model:
  name: unsloth/Llama-3.2-1B-Instruct-bnb-4bit
  name_full: meta-llama/Llama-3.2-1B-Instruct
  family: llama
  max_seq_length: 131072
  chat_template: llama3

lora:
  r: 16
  lora_alpha: 16
  target_modules:
    - q_proj
    - k_proj
    - v_proj
    - o_proj
    - gate_proj
    - up_proj
    - down_proj
  lora_dropout: 0
  bias: none
  use_gradient_checkpointing: unsloth
